/**
 * External URL that must use the HTTPS protocol.
 *
 * Commonly used for static configuration values such as
 * websites, social links, and documentation and external resources.
 */
export type ExternalUrl = `https://${string}`;
