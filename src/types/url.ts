/**
 * Represents a secure HTTPS URL.
 *
 * Intended for static configuration values such as
 * company websites, social links, and external resources.
 */
export type ExternalUrl = `https://${string}`;
